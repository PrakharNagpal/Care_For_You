<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.health">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".Prescription_1"></activity>
        <activity android:name=".Appointments" />
        <activity android:name=".Consultations" />
        <activity android:name=".Medicines" />
        <activity android:name=".AllPrescriptions" />
        <activity android:name=".MeetNewDoctor" />
        <activity android:name=".IntroScreen" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>